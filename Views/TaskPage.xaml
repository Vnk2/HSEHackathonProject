<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HSEHackathonProject.Views.TaskPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HSEHackathonProject.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="25" RowSpacing="10" ColumnSpacing="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Spacing="10" Grid.Row="0" Grid.ColumnSpan="2">
            <TextBlock Name="TaskNoTextBlock" Text="Задача № 123"
                       FontSize="20" FontWeight="Bold"/>
            <Image Name="TaskImage" 
                   HorizontalAlignment="Center" VerticalAlignment="Top"
                   MaxHeight="100"/>
        </StackPanel>


        <Image Name="AnswerImage" Grid.Row="2"
               HorizontalAlignment="Center" VerticalAlignment="Bottom"
               Visibility="Collapsed" MaxHeight="100" />
        <Button Name="ShowAnswerButton" Grid.Row="3" 
                Style="{StaticResource AccentButtonStyle}"
                HorizontalAlignment="Stretch"
                Click="ShowAnswerButton_Click">
            <StackPanel Spacing="10" Orientation="Horizontal">
                <SymbolIcon Symbol="Accept" />
                <TextBlock Text="Показать ответ"/>
            </StackPanel>
        </Button>
    </Grid>
</Page>
