<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HSEHackathonProject.Views.TasksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HSEHackathonProject.Views"
    xmlns:models="using:HSEHackathonProject.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Name="TaskTemplate" x:DataType="TreeViewNode">
            <ItemContainer>
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <SymbolIcon Symbol="Library"/>
                    <TextBlock Text="{x:Bind Content.ToString()}"/>
                </StackPanel>
            </ItemContainer>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <TreeView Name="TasksView" ItemTemplate="{StaticResource TaskTemplate}"
                  SelectionMode="None" ItemInvoked="TasksView_ItemInvoked"/>
    </Grid>
</Page>
